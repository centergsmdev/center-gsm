import{plainText}from"@/lib/seo/helpers";export const safeSocialText=(value:string|undefined,fallback:string,max=200)=>plainText(value??fallback,max)||fallback;export const normalizeSocialImages=(values:(string|undefined)[],fallback:string)=>[...new Set(values.filter(Boolean).map(x=>x as string))].slice(0,4).map(url=>({url,alt:"CENTER GSM sosyal medya önizlemesi"})).concat(values.some(Boolean)?[]:[{url:fallback,alt:"CENTER GSM sosyal medya önizlemesi"}]);
