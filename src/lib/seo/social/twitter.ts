import type{Metadata}from"next";import{SEO_CONFIG}from"@/lib/seo/constants";import{socialImageUrl,DEFAULT_TWITTER_IMAGE}from"./images";import{safeSocialText}from"./helpers";export type TwitterInput={title?:string;description?:string;image?:string};export function createTwitterCard(input:TwitterInput={}):NonNullable<Metadata["twitter"]>{const title=safeSocialText(input.title,SEO_CONFIG.defaultTitle,100);return{card:"summary_large_image",title,description:safeSocialText(input.description,SEO_CONFIG.defaultDescription,200),images:[{url:socialImageUrl(input.image,DEFAULT_TWITTER_IMAGE),alt:title}],creator:SEO_CONFIG.twitterHandle,site:SEO_CONFIG.twitterHandle}}
