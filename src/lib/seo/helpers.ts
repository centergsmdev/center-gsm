export function plainText(value:string|undefined|null,max=160){return(value??"").replace(/<[^>]*>/g," ").replace(/\s+/g," ").trim().slice(0,max).trim()}export function uniqueKeywords(values:(string|undefined|null)[]){return[...new Set(values.flatMap(v=>(v??"").split(",")).map(v=>v.trim()).filter(Boolean))]}export function taxonomyDescription(name:string,type:"kategori"|"marka"){return type==="kategori"?`${name} kategorisindeki seçili teknoloji ürünlerini CENTER GSM güvencesiyle keşfedin.`:`${name} ürünlerini, güncel fiyatları ve seçili teknoloji fırsatlarını CENTER GSM'de inceleyin.`}
