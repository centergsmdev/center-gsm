import{canonicalUrl}from"@/lib/seo/canonical";import{SEO_CONFIG}from"@/lib/seo/constants";import type{CatalogProduct}from"@/types/product";import{absoluteAssetUrl}from"./helpers";import type{JsonLdObject}from"./types";export function createProductSchema(product:CatalogProduct):JsonLdObject{const available=product.availableStock!==undefined?product.availableStock>0:product.stockStatus!=="out-of-stock",images=(product.imageUrls?.length?product.imageUrls:product.mainImageUrl?[product.mainImageUrl]:[SEO_CONFIG.organizationLogo]).map(absoluteAssetUrl),price=Number.isFinite(product.price)&&product.price>=0?product.price:0;return{"@context":"https://schema.org","@type":"Product",name:`${product.brand} ${product.model}`,sku:product.sku??product.id,brand:{"@type":"Brand",name:product.brand},category:product.category,description:product.description,image:images,url:canonicalUrl(`/urun/${product.slug}`),offers:{"@type":"Offer",url:canonicalUrl(`/urun/${product.slug}`),price:price.toFixed(2),priceCurrency:"TRY",availability:`https://schema.org/${available?"InStock":"OutOfStock"}`,itemCondition:"https://schema.org/NewCondition",seller:{"@type":"Organization",name:SEO_CONFIG.organizationName,url:canonicalUrl("/")}}}}
